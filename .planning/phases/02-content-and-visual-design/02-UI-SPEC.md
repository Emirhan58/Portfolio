---
phase: 2
slug: content-and-visual-design
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-25
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for all 7 portfolio sections with Japanese visual treatment. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (hand-built with Tailwind CSS 4) |
| Icon library | none (inline SVG, emoji flags, CSS shapes) |
| Font (heading) | Playfair Display, Georgia, serif (`--font-heading`) |
| Font (kanji) | Noto Serif JP, serif (`--font-kanji`) — subset to 7 chars |
| Font (body) | Inter, system-ui, sans-serif (`--font-body`) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline element margins, tag pill padding-y |
| sm | 8px | Compact element spacing, tag pill padding-x, card inner gaps, button padding-y |
| md | 16px | Default element spacing, card padding, button padding-x |
| lg | 24px | Card padding (large), section content gaps |
| xl | 32px | Layout gaps between cards, timeline card margins |
| 2xl | 48px (`--spacing-section-mobile`) | Section padding on mobile |
| 3xl | 64px (`--spacing-section`) | Section padding on desktop |

Exceptions:
- 44px minimum touch target on all interactive elements (buttons, links, card hover zones) — accessibility standard, not a spacing token

---

## Typography

| Role | Size | Weight | Line Height | Font Family |
|------|------|--------|-------------|-------------|
| Display | `clamp(3rem, 5vw, 6rem)` (`--text-display`) | 700 (bold) | 1.1 | `--font-heading` |
| Heading | 2.25rem / 36px (`--text-heading`) | 700 (bold) | 1.2 | `--font-heading` |
| Subheading | 1.5rem / 24px (`--text-subheading`) | 700 (bold) | 1.3 | `--font-heading` |
| Body | 1rem / 16px (`--text-body`) | 400 (regular) | 1.6 | `--font-body` |

Weights used in this phase: 400 (regular) and 700 (bold). No other weights.

**Scale notes:**
- Display is used exclusively for the hero name. The `clamp()` function provides responsive sizing from 48px to 96px.
- Heading (36px) is used for all section titles (About, Skills, Experience, Projects, Achievements, Contact).
- Subheading (24px) is used for card titles, company names, skill category headers, and achievement names.
- Body (16px) is used for all body text, meta info, durations, labels, tech tags, and supporting copy. Where a smaller visual weight is needed, reduce opacity or use `--color-text-secondary` rather than a smaller font size.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0a0a0a` (`--color-bg`) | Page background, odd section backgrounds |
| Secondary (30%) | `#1a1a1a` (`--color-surface`) | Even section backgrounds, card surfaces, timeline track |
| Accent Red (5%) | `#c0392b` (`--color-accent-red`) | See reserved list below |
| Accent Gold (5%) | `#d4a574` (`--color-accent-gold`) | See reserved list below |
| Text Primary | `#f5f0e8` (`--color-paper`) | Headings, hero name, primary body text |
| Text Secondary | `#a0a0a0` (`--color-text-secondary`) | Supporting text, role/title subtitles, meta info |
| Accent Red Bright | `#e74c3c` (`--color-accent-red-bright`) | Hover state for red accent elements only |
| Bamboo | `#2d5016` (`--color-bamboo`) | Footer background accent only |

### Accent Red reserved for:
- Download CV button (primary CTA) background
- Proficiency bar fill inside skill cards
- Timeline dot markers
- Active nav link text + underline (already in Phase 1)
- Katana slash section dividers at 20-30% opacity
- Contact icon hover glow

### Accent Gold reserved for:
- Section heading decorative elements
- Achievement medal/badge borders
- Contact mon (crest) icon borders
- EK monogram in navbar (already in Phase 1)
- Card subtle border accents (1px, 10-15% opacity)
- Achievement medal metallic gradient: `linear-gradient(135deg, #d4a574, #c49a6c, #d4a574)`

---

## Section Visual Contracts

### Hero Section
- Layout: centered vertically and horizontally, 90vh height (from SectionShell)
- Name: Playfair Display, `clamp(3rem, 5vw, 6rem)`, `--color-paper`, `font-weight: 700`
- Role subtitle: `--text-subheading`, `--color-text-secondary`, opacity 0.8
- Tagline: `--text-body`, `--color-text-secondary`, italic, "Crafting robust systems with precision and purpose"
- CTA: `bg-accent-red text-paper`, padding `8px 24px`, `border-radius: 6px`, hover `bg-accent-red-bright` with `box-shadow: 0 0 20px rgba(192, 57, 43, 0.4)`
- Scroll indicator: chevron at bottom center, CSS bounce animation (1.5s infinite), fades out after 100px scroll
- Background: faint sumi-e radial texture from center, 5-8% opacity over `--color-bg`
- No profile photo in hero

### About Section
- Background: `--color-surface` (even section)
- Photo: 200px circle on desktop, CSS mask with organic brush-stroke SVG clip-path for ink-wash frame
- Layout: photo left + text right on desktop (grid 5/7 split), stacked on mobile (photo centered above text)
- Stats: 3 counters in a row, each showing number (`--text-heading`, `--color-accent-red`, weight 700) + label (`--text-body`, `--color-text-secondary`)
- Stats values: "1+", "39+", "4" with labels "Years Experience", "Projects", "Companies"
- Education: `--text-body`, `--color-text-secondary`, below bio text
- Bio: `--text-body`, `--color-paper`, max-width 600px, `line-height: 1.6`

### Skills Section
- Background: `--color-bg` (odd section)
- Layout: 2-column grid on desktop, 1-column on mobile
- 4 parchment cards, each with:
  - Surface: `--color-surface` with paper grain texture at 5% opacity
  - Border: `1px solid rgba(212, 165, 116, 0.15)` (gold at 15% opacity)
  - Border-radius: `8px`
  - Padding: `24px`
  - Card title: `--text-subheading`, `--color-paper`, weight 700
  - Skill items: `--text-body`, `--color-text-secondary`
  - Proficiency bar: 6px height, `--color-surface` track (darker), `--color-accent-red` fill, `border-radius: 3px`
  - Rolled-edge CSS effect: `box-shadow: inset 0 2px 4px rgba(0,0,0,0.3), inset 0 -2px 4px rgba(0,0,0,0.2)` on top/bottom edges

### Experience Section
- Background: `--color-surface` (even section)
- Timeline: vertical dashed line, `1px dashed rgba(160, 160, 160, 0.3)`, centered on desktop
- Timeline dots: 12px circles, `--color-accent-red`, centered on timeline line
- Cards: alternating left/right of timeline on desktop, all left-aligned on mobile
- Card surface: `--color-bg` (contrast against section surface)
- Card padding: `24px`, border-radius `8px`
- Card border-left: `3px solid rgba(192, 57, 43, 0.5)` on mobile (no alternation)
- Company name: `--text-subheading`, `--color-paper`, weight 700
- Role: `--text-body`, `--color-accent-gold`
- Duration: `--text-body`, `--color-text-secondary`
- Bullets: `--text-body`, `--color-text-secondary`, line-height 1.6
- Tech tags: inline pills, `--text-body`, `--color-text-secondary`, `background: rgba(192, 57, 43, 0.1)`, `border: 1px solid rgba(192, 57, 43, 0.2)`, `border-radius: 4px`, padding `4px 8px`
- All 5 positions, same card size, most recent first
- Mobile: timeline collapses to left-aligned cards with left border, no center line

### Projects Section
- Background: `--color-bg` (odd section)
- Layout: masonry-style grid, 3 columns on desktop (>=1024px), 2 columns on tablet (>=768px), 1 column on mobile
- 6 parchment cards with:
  - Surface: `--color-surface` with paper grain at 5% opacity
  - Border: `1px solid rgba(212, 165, 116, 0.1)`
  - Border-radius: `8px`
  - Padding: `24px`
  - Project name: `--text-subheading`, `--color-paper`, weight 700
  - Description: `--text-body`, `--color-text-secondary`, max 3 lines with `line-clamp-3`
  - Tech tags: same pill style as Experience section
  - GitHub link: `--text-body`, `--color-accent-red`, underline on hover
  - Star count (where applicable): `--text-body`, `--color-accent-gold`, inline with GitHub link
  - Hover: CSS-only ink-wash overlay, `background: linear-gradient(to bottom, transparent 0%, rgba(10, 10, 10, 0.85) 40%)` reveals full description (replaces line-clamp), transition `300ms`
- Column gap: `24px`, row gap: `24px`

### Achievements Section
- Background: `--color-surface` (even section)
- Layout: 2 medal cards centered, max-width `800px`, flex row on desktop, stacked on mobile
- Medal styling:
  - Circular top element: `120px` diameter, `border: 3px solid`, `border-image: linear-gradient(135deg, #d4a574, #c49a6c, #d4a574) 1` or solid gold border with metallic effect via `box-shadow`
  - Alternative: `border: 3px solid #d4a574` with `box-shadow: 0 0 15px rgba(212, 165, 116, 0.2), inset 0 0 15px rgba(212, 165, 116, 0.1)`
  - Card below medal: `--color-bg` surface, padding `24px`, border-radius `8px`
  - Achievement name: `--text-subheading`, `--color-paper`, weight 700
  - Description: `--text-body`, `--color-text-secondary`
  - Year/event: `--text-body`, `--color-accent-gold`

### Contact Section
- Background: `--color-bg` (odd section)
- Layout: centered, max-width `600px`
- Section heading: `--text-heading`, `--color-paper`, centered
- 3 mon (crest) icons in a row, centered:
  - Container: `64px` circles, `border: 2px solid #d4a574`, `border-radius: 50%`
  - Icon inside: inline SVG (email, GitHub, LinkedIn), `--color-paper`, 24px size
  - Hover: `border-color: --color-accent-red`, `box-shadow: 0 0 20px rgba(192, 57, 43, 0.3)`, transition `300ms`
  - Below each icon: label text, `--text-body`, `--color-text-secondary`
- Links: `--color-paper`, hover `--color-accent-red`
- Email: emirhan.kaya_58@hotmail.com
- GitHub: github.com/Emirhan58
- LinkedIn: linkedin.com/in/emirhankaya

### Section Dividers
- Thin red diagonal katana slash between every section
- Implementation: pseudo-element `::after` on each section, `height: 2px`, `background: --color-accent-red` at 20-30% opacity, `transform: rotate(-2deg)`, full width
- No animation in Phase 2 (Phase 3 adds scroll-triggered slash animation)

### Visual Textures
- Paper grain: repeating noise texture PNG/SVG, 5-10% opacity, applied via `background-image` on section backgrounds
- Alternating backgrounds: odd sections `--color-bg`, even sections `--color-surface`
- Asanoha/seigaiha patterns: used ONLY as section divider strip decoration (not on cards or backgrounds), SVG pattern, 5-8% opacity
- Ink-wash elements: limited to hero background, About photo frame, and card edge shadows
- Decorative kanji characters (門, 道, 技, 戦, 作, 誉, 結): rendered at 8rem on mobile / 12rem on desktop, `--font-kanji` (Noto Serif JP), `--color-accent-gold` at 10% opacity, positioned top-right of each section. These are one-off decorative elements and do not participate in the typography scale. Already implemented in Phase 1 SectionShell with `aria-hidden="true"`.

---

## Copywriting Contract

| Element | Copy (EN) | Copy (TR) |
|---------|-----------|-----------|
| Primary CTA | "Download CV" | "CV Indir" |
| Hero name | "Emirhan Kaya" | "Emirhan Kaya" |
| Hero role | "Full-Stack Developer" | "Full-Stack Developer" |
| Hero tagline | "Crafting robust systems with precision and purpose" | "Saglam sistemleri hassasiyet ve amacla insa etmek" |
| Section heading: About | "About" | "Hakkimda" |
| Section heading: Skills | "Skills" | "Yetenekler" |
| Section heading: Experience | "Experience" | "Deneyim" |
| Section heading: Projects | "Projects" | "Projeler" |
| Section heading: Achievements | "Achievements" | "Basarilar" |
| Section heading: Contact | "Contact" | "Iletisim" |
| Empty state (projects) | Not applicable — all 6 projects are static content | N/A |
| Error state | Not applicable — static content, no data fetching | N/A |
| Destructive actions | None in this phase | N/A |

All copy flows through `next-intl` message keys in `en.json` / `tr.json`. Structural parity required between both locale files.

---

## Interaction Contracts

| Interaction | Behavior | Duration |
|-------------|----------|----------|
| Card hover (projects) | Ink-wash overlay fades in, reveals full description | `300ms` (`--duration-normal`) |
| CTA button hover | Background shifts `accent-red` to `accent-red-bright`, glow shadow appears | `300ms` |
| Contact icon hover | Border color gold to red, red glow shadow | `300ms` |
| Nav link hover | Text color `text-secondary` to `text-primary` | `300ms` |
| Scroll indicator bounce | CSS `translateY` bounce, infinite, pauses on scroll | `1500ms` per cycle |
| Proficiency bars | Static fill in Phase 2 (Phase 3 adds scroll-triggered animation) | N/A |
| Stat counters | Static numbers in Phase 2 (Phase 3 adds counting animation) | N/A |

All hover interactions gated by `prefers-reduced-motion`: if reduced, transitions set to `0.01ms` per global CSS rule.

---

## Responsive Breakpoints

| Breakpoint | Width | Layout Changes |
|------------|-------|----------------|
| Mobile | < 768px | Single column, stacked cards, timeline left-aligned, 1-col masonry |
| Tablet | >= 768px | 2-col project grid, timeline still left-aligned |
| Desktop | >= 1024px | 2-col skills, 3-col projects, alternating timeline, photo-left About layout |
| Wide | >= 1280px | Max-width 7xl (1280px) container, centered |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| Third-party | none | not applicable |

No component registry in use. All components are hand-built with Tailwind CSS 4.

---

## Accessibility Contracts

| Requirement | Implementation |
|-------------|----------------|
| Color contrast | All text on dark backgrounds meets WCAG AA (paper #f5f0e8 on #0a0a0a = 15.4:1, secondary #a0a0a0 on #0a0a0a = 7.5:1) |
| Red accent on dark | #c0392b on #0a0a0a = 5.1:1 (passes AA for normal text) |
| Gold on dark | #d4a574 on #0a0a0a = 6.2:1 (passes AA) |
| Touch targets | All buttons and links minimum 44px touch area |
| Decorative kanji | `aria-hidden="true"` (already set in SectionShell) |
| Alt text | Profile photo requires meaningful alt text in both locales |
| Reduced motion | Global CSS rule disables all transitions/animations |
| Focus indicators | `outline: 2px solid #c0392b`, `outline-offset: 2px` on all focusable elements |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
