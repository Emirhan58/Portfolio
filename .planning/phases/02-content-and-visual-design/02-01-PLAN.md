---
phase: 02-content-and-visual-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/fonts.ts
  - src/lib/data.ts
  - src/messages/en.json
  - src/messages/tr.json
  - src/components/ui/ParchmentCard.tsx
  - src/components/ui/ProficiencyBar.tsx
  - src/components/ui/TechTag.tsx
  - src/components/ui/MedalBadge.tsx
  - src/components/ui/MonIcon.tsx
  - src/components/layout/SectionDivider.tsx
  - src/app/[locale]/page.tsx
  - src/app/globals.css
autonomous: true
requirements: [VISL-01, VISL-02, VISL-03, VISL-04]

must_haves:
  truths:
    - "Noto Serif JP renders correctly for all 7 kanji characters in SectionShell"
    - "Paper grain texture is visible at subtle opacity on section backgrounds"
    - "Katana slash dividers appear between every section"
    - "Asanoha/seigaiha geometric pattern strips decorate section dividers"
    - "Sections alternate between bg (#0a0a0a) and surface (#1a1a1a) backgrounds"
    - "All 7 section content is translatable via en.json and tr.json"
  artifacts:
    - path: "src/lib/data.ts"
      provides: "Non-translatable portfolio data (URLs, star counts, proficiency percentages, tech arrays)"
      exports: ["SKILLS_DATA", "EXPERIENCE_DATA", "PROJECTS_DATA", "ACHIEVEMENTS_DATA", "CONTACT_LINKS", "STATS_DATA"]
    - path: "src/messages/en.json"
      provides: "All English content for 7 sections"
      contains: "about"
    - path: "src/messages/tr.json"
      provides: "All Turkish content for 7 sections"
      contains: "about"
    - path: "src/components/ui/ParchmentCard.tsx"
      provides: "Reusable parchment-styled card with paper grain and gold border"
      exports: ["ParchmentCard"]
    - path: "src/components/ui/ProficiencyBar.tsx"
      provides: "Katana-styled proficiency bar component"
      exports: ["ProficiencyBar"]
    - path: "src/components/ui/TechTag.tsx"
      provides: "Ink-stamp tech tag pill"
      exports: ["TechTag"]
    - path: "src/components/ui/MedalBadge.tsx"
      provides: "Achievement medal/badge with gold gradient border"
      exports: ["MedalBadge"]
    - path: "src/components/ui/MonIcon.tsx"
      provides: "Mon crest-styled social link icon"
      exports: ["MonIcon"]
    - path: "src/components/layout/SectionDivider.tsx"
      provides: "Katana slash + geometric pattern section divider"
      exports: ["SectionDivider"]
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/layout/SectionDivider.tsx"
      via: "import and render between sections"
      pattern: "SectionDivider"
    - from: "src/messages/en.json"
      to: "src/messages/tr.json"
      via: "structural parity — identical key paths"
      pattern: "about.*bio"
---

<objective>
Create the complete data foundation, i18n content, shared UI component library, and visual texture system that all 7 section components depend on.

Purpose: Every section in Plans 02 and 03 will import from data.ts, use i18n messages, and compose shared UI components. Building these first prevents duplicate work and ensures consistent visual treatment across sections.

Output: data.ts with typed portfolio data, en.json/tr.json with all section content, 5 shared UI components, SectionDivider with katana slash + geometric patterns, page.tsx rewired with alternating backgrounds, and Noto Serif JP font fix for kanji rendering.
</objective>

<execution_context>
@.planning/phases/02-content-and-visual-design/02-CONTEXT.md
@.planning/phases/02-content-and-visual-design/02-RESEARCH.md
@.planning/phases/02-content-and-visual-design/02-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@EMIRHAN_KAYA_DATA.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From src/lib/constants.ts:
```typescript
export const SECTION_IDS = ["hero", "about", "skills", "experience", "projects", "achievements", "contact"] as const;
export type SectionId = (typeof SECTION_IDS)[number];
export const SECTION_KANJI: Record<SectionId, string> = { hero: "\u9580", about: "\u9053", skills: "\u6280", experience: "\u6226", projects: "\u4F5C", achievements: "\u8A89", contact: "\u7D50" };
export const NAV_LINKS: { id: SectionId; label: string }[] = [/*...*/];
```

From src/components/layout/SectionShell.tsx:
```typescript
interface SectionShellProps { id: SectionId; children: React.ReactNode; className?: string; showKanji?: boolean; }
export function SectionShell({ id, children, className, showKanji = true }: SectionShellProps): JSX.Element;
```

From src/lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/app/globals.css @theme:
```css
--color-bg: #0a0a0a; --color-surface: #1a1a1a; --color-accent-red: #c0392b;
--color-accent-red-bright: #e74c3c; --color-accent-gold: #d4a574;
--color-paper: #f5f0e8; --color-text-secondary: #a0a0a0;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Font fix, i18n messages, and portfolio data module</name>
  <files>
    src/app/fonts.ts,
    src/lib/data.ts,
    src/messages/en.json,
    src/messages/tr.json
  </files>
  <read_first>
    src/app/fonts.ts,
    src/messages/en.json,
    src/messages/tr.json,
    src/lib/constants.ts,
    EMIRHAN_KAYA_DATA.md,
    .planning/phases/02-content-and-visual-design/02-UI-SPEC.md,
    .planning/phases/02-content-and-visual-design/02-CONTEXT.md
  </read_first>
  <action>
**1. Fix Noto Serif JP font subsetting (src/app/fonts.ts):**

Replace the current `notoSerifJP` export. The `subsets: ["latin"]` does NOT include Japanese kanji. Change to:

```typescript
export const notoSerifJP = Noto_Serif_JP({
  weight: ["400", "700"],
  subsets: ["latin"],
  display: "swap",
  variable: "--font-noto-serif-jp",
  preload: false,
  text: "\u9580\u9053\u6280\u6226\u4F5C\u8A89\u7D50",
});
```

The `text` parameter creates a micro-subset containing exactly the 7 decorative kanji characters. Keep everything else in fonts.ts unchanged.

**2. Create src/lib/data.ts with all non-translatable portfolio data:**

This file holds structural data that does NOT change between locales (URLs, numbers, arrays, percentages). All display text goes through i18n messages.

```typescript
// Skill proficiency percentages (Claude's discretion per CONTEXT.md)
export const SKILLS_DATA = {
  languages: [
    { key: "java", proficiency: 90 },
    { key: "kotlin", proficiency: 85 },
    { key: "csharp", proficiency: 75 },
    { key: "php", proficiency: 70 },
    { key: "javascript", proficiency: 80 },
    { key: "python", proficiency: 70 },
    { key: "cpp", proficiency: 65 },
  ],
  frameworks: [
    { key: "springboot", proficiency: 90 },
    { key: "dotnet", proficiency: 75 },
    { key: "react", proficiency: 70 },
    { key: "laravel", proficiency: 65 },
  ],
  infrastructure: [
    { key: "docker", proficiency: 85 },
    { key: "kubernetes", proficiency: 75 },
    { key: "helm", proficiency: 70 },
    { key: "aws", proficiency: 65 },
    { key: "cicd", proficiency: 80 },
    { key: "flyway", proficiency: 80 },
    { key: "prometheus", proficiency: 70 },
    { key: "grafana", proficiency: 70 },
  ],
  security: [
    { key: "rabbitmq", proficiency: 80 },
    { key: "kafka", proficiency: 80 },
    { key: "redis", proficiency: 85 },
    { key: "jwt", proficiency: 90 },
    { key: "mtls", proficiency: 70 },
    { key: "keycloak", proficiency: 65 },
    { key: "oauth", proficiency: 80 },
  ],
} as const;

export type SkillCategory = keyof typeof SKILLS_DATA;

export const EXPERIENCE_DATA = [
  {
    key: "seyfhnova",
    tech: ["Java", "Kotlin", "Spring Boot", "Redis", "Kafka", "RabbitMQ", "Docker"],
  },
  {
    key: "planbanimation",
    tech: ["Java", "Kotlin", "Spring Boot", "Redis", "Kafka", "Docker", "Kubernetes"],
  },
  {
    key: "tenexy",
    tech: ["E-Commerce", "Stripe", "Authentication", "Database Design"],
  },
  {
    key: "gesk",
    tech: ["C/C++", "STM32", "Embedded Systems", "TouchGFX"],
  },
  {
    key: "kplus",
    tech: [".NET", "JavaScript", "REST API"],
  },
] as const;

export const PROJECTS_DATA = [
  {
    key: "microservices",
    github: "https://github.com/Emirhan58/microservices-java",
    stars: 3,
    tech: ["Java", "Spring Boot", "Microservices", "Docker"],
  },
  {
    key: "flappybird",
    github: "https://github.com/Emirhan58/STM32-FLAPPY-BIRD",
    stars: 10,
    tech: ["C++", "STM32", "TouchGFX", "Embedded"],
  },
  {
    key: "behaviorwatch",
    github: "https://github.com/Emirhan58/behaviorwatch-ai",
    stars: 0,
    tech: ["Python", "YOLOv8", "OpenCV", "AI"],
  },
  {
    key: "bunqchat",
    github: "https://github.com/Emirhan58/bunq-chat-app-backend",
    stars: 0,
    tech: ["PHP", "REST API", "Backend"],
  },
  {
    key: "allinpro",
    github: "https://github.com/Emirhan58/allinpro",
    stars: 0,
    tech: ["PHP", "E-Commerce", "MySQL", "Payments"],
  },
  {
    key: "cansat",
    github: "https://github.com/Emirhan58/CANSAT-GROUND-CONTROL-SYSTEM",
    stars: 0,
    tech: ["Python", "PyQt5", "Telemetry"],
  },
] as const;

export const ACHIEVEMENTS_DATA = [
  { key: "teknofest" },
  { key: "cansat" },
] as const;

export const CONTACT_LINKS = {
  email: { href: "mailto:emirhan.kaya_58@hotmail.com", display: "emirhan.kaya_58@hotmail.com" },
  github: { href: "https://github.com/Emirhan58", display: "github.com/Emirhan58" },
  linkedin: { href: "https://linkedin.com/in/emirhankaya", display: "linkedin.com/in/emirhankaya" },
} as const;

export const STATS_DATA = [
  { key: "experience", value: "1+" },
  { key: "projects", value: "39+" },
  { key: "companies", value: "4" },
] as const;
```

**3. Populate en.json with all section content:**

Keep existing `nav`, `hero`, `meta` keys. Add sections below. The hero section needs an update to include `downloadCV` key. Full en.json structure:

```json
{
  "nav": { /* keep existing */ },
  "hero": {
    "name": "Emirhan Kaya",
    "role": "Full-Stack Developer",
    "tagline": "Crafting robust systems with precision and purpose",
    "downloadCV": "Download CV",
    "scrollDown": "Scroll down"
  },
  "meta": { /* keep existing */ },
  "about": {
    "heading": "About",
    "bio": "Back-End Developer specializing in high-performance and reliable systems with Java/Spring Boot and .NET. Crafting E-Commerce systems and event-driven architectures.",
    "photoAlt": "Emirhan Kaya profile photo",
    "stats": {
      "experience": "Years Experience",
      "projects": "Projects",
      "companies": "Companies"
    },
    "education": {
      "degree": "Computer Engineering",
      "university": "Sakarya University",
      "period": "2020 - 2024",
      "gpa": "GPA: 3.31"
    }
  },
  "skills": {
    "heading": "Skills",
    "categories": {
      "languages": "Languages",
      "frameworks": "Frameworks",
      "infrastructure": "Infrastructure & DevOps",
      "security": "Messaging & Security"
    },
    "items": {
      "java": "Java",
      "kotlin": "Kotlin",
      "csharp": "C#",
      "php": "PHP",
      "javascript": "JavaScript",
      "python": "Python",
      "cpp": "C/C++",
      "springboot": "Spring Boot",
      "dotnet": ".NET",
      "react": "React",
      "laravel": "Laravel",
      "docker": "Docker",
      "kubernetes": "Kubernetes",
      "helm": "Helm",
      "aws": "AWS S3",
      "cicd": "CI/CD",
      "flyway": "Flyway",
      "prometheus": "Prometheus",
      "grafana": "Grafana",
      "rabbitmq": "RabbitMQ",
      "kafka": "Kafka",
      "redis": "Redis",
      "jwt": "JWT",
      "mtls": "mTLS",
      "keycloak": "KeyCloak",
      "oauth": "OAuth"
    }
  },
  "experience": {
    "heading": "Experience",
    "positions": {
      "seyfhnova": {
        "company": "Seyfhnova",
        "role": "Full-Stack Developer (Freelancer)",
        "period": "Jun 2025 - Present",
        "bullets": [
          "Kotlin + Spring Boot microservices, 12/15-factor architecture",
          "JWT auth, X-Service-Token API security",
          "Redis caching, Flyway schema versioning",
          "RabbitMQ & Kafka event-driven communication"
        ]
      },
      "planbanimation": {
        "company": "Plan B Animation and Game Studio",
        "role": "Backend Developer",
        "period": "Jun 2025 - Feb 2026",
        "location": "Ankara",
        "bullets": [
          "Backend infrastructure for multiple client projects",
          "Production-ready services from design to deployment to monitoring",
          "Led and mentored 3-person junior backend team"
        ]
      },
      "tenexy": {
        "company": "Tenexy",
        "role": "Backend Developer (Freelancer)",
        "period": "Jun 2022 - Jun 2025",
        "bullets": [
          "E-commerce solutions, multi-language support, Stripe/Sellix payment",
          "Authentication systems, advanced cart features",
          "Database architecture and query optimization"
        ]
      },
      "gesk": {
        "company": "GESK Technologies",
        "role": "Intern, Embedded Software Developer",
        "period": "Sep 2023 - Oct 2023",
        "location": "Istanbul",
        "bullets": [
          "Flappy Bird game on STM32 (C/C++)",
          "Real-time programming, hardware-software integration"
        ]
      },
      "kplus": {
        "company": "KPlus Information Technologies",
        "role": "Intern, Fullstack Developer",
        "period": "Jul 2023 - Aug 2023",
        "location": "Istanbul",
        "bullets": [
          "UBER-like ride-sharing application (.NET)",
          "Real-time features, geolocation, RESTful API"
        ]
      }
    }
  },
  "projects": {
    "heading": "Projects",
    "items": {
      "microservices": {
        "name": "Microservices Java",
        "description": "Spring Boot microservices, scalable distributed architecture. Service discovery, API gateway, event-driven communication.",
        "shortDescription": "Spring Boot microservices with service discovery and event-driven communication"
      },
      "flappybird": {
        "name": "STM32 Flappy Bird",
        "description": "Interactive Flappy Bird on STM32F429I-DISC0. TouchGFX graphics, real-time game loop with hardware integration.",
        "shortDescription": "Interactive Flappy Bird on STM32 with TouchGFX real-time game loop"
      },
      "behaviorwatch": {
        "name": "BehaviorWatch AI",
        "description": "Real-time cigarette detection system using YOLOv8 and OpenCV for behavioral monitoring.",
        "shortDescription": "Real-time cigarette detection with YOLOv8 and OpenCV"
      },
      "bunqchat": {
        "name": "Bunq Chat Backend",
        "description": "Real-time chat application for Bunq, built with clean architecture and RESTful API design.",
        "shortDescription": "Real-time chat backend with clean architecture"
      },
      "allinpro": {
        "name": "AllInPro E-Commerce",
        "description": "Full-featured e-commerce platform with payment integration, product management, and order processing.",
        "shortDescription": "Full-featured e-commerce platform with payment integration"
      },
      "cansat": {
        "name": "CanSat Ground Control",
        "description": "Ground station for NASA/Lockheed Martin coordinated CanSat competition. Real-time telemetry, GPS tracking, altitude monitoring.",
        "shortDescription": "CanSat ground station with real-time telemetry and GPS tracking"
      }
    }
  },
  "achievements": {
    "heading": "Achievements",
    "items": {
      "teknofest": {
        "name": "TEKNOFEST 2021 & 2022",
        "subtitle": "Finalist",
        "description": "Image processing, UAV autonomous flight, and rocket competition ground station design"
      },
      "cansat": {
        "name": "CanSat Competition",
        "subtitle": "Finalist",
        "description": "ST FOCOUNT team ground station management, coordinated by NASA & Lockheed Martin"
      }
    }
  },
  "contact": {
    "heading": "Contact",
    "email": "Email",
    "github": "GitHub",
    "linkedin": "LinkedIn"
  }
}
```

**4. Populate tr.json with identical structure, Turkish translations:**

Keep existing `nav`, `hero`, `meta`. Update hero to match en.json structure. Add all sections with Turkish content:

```json
{
  "nav": { /* keep existing */ },
  "hero": {
    "name": "Emirhan Kaya",
    "role": "Full-Stack Developer",
    "tagline": "Saglam sistemleri hassasiyet ve amacla insa etmek",
    "downloadCV": "CV Indir",
    "scrollDown": "Asagi kaydir"
  },
  "meta": { /* keep existing */ },
  "about": {
    "heading": "Hakkimda",
    "bio": "Java/Spring Boot ve .NET ile yuksek performansli ve guvenilir sistemler gelistiren Back-End Developer. E-Ticaret sistemleri ve event-driven mimariler olusturuyor.",
    "photoAlt": "Emirhan Kaya profil fotografi",
    "stats": {
      "experience": "Yil Deneyim",
      "projects": "Proje",
      "companies": "Sirket"
    },
    "education": {
      "degree": "Bilgisayar Muhendisligi",
      "university": "Sakarya Universitesi",
      "period": "2020 - 2024",
      "gpa": "GNO: 3.31"
    }
  },
  "skills": {
    "heading": "Yetenekler",
    "categories": {
      "languages": "Programlama Dilleri",
      "frameworks": "Framework ve Kutuphaneler",
      "infrastructure": "Altyapi ve DevOps",
      "security": "Mesajlasma ve Guvenlik"
    },
    "items": { /* same as en.json — tech names are universal */ }
  },
  "experience": {
    "heading": "Deneyim",
    "positions": {
      "seyfhnova": {
        "company": "Seyfhnova",
        "role": "Full-Stack Developer (Serbest)",
        "period": "Haz 2025 - Devam Ediyor",
        "bullets": [
          "Kotlin + Spring Boot mikroservisler, 12/15-factor mimarisi",
          "JWT auth, X-Service-Token ile API guvenligi",
          "Redis cache, Flyway sema versiyonlama",
          "RabbitMQ ve Kafka event-driven iletisim"
        ]
      },
      "planbanimation": {
        "company": "Plan B Animation and Game Studio",
        "role": "Backend Developer",
        "period": "Haz 2025 - Sub 2026",
        "location": "Ankara",
        "bullets": [
          "Birden fazla musteri projesinde backend altyapisi",
          "Tasarimdan deployment ve izlemeye uretim hazir servisler",
          "3 kisilik junior backend takimina liderlik ve mentorluk"
        ]
      },
      "tenexy": {
        "company": "Tenexy",
        "role": "Backend Developer (Serbest)",
        "period": "Haz 2022 - Haz 2025",
        "bullets": [
          "E-ticaret cozumleri, coklu dil destegi, Stripe/Sellix odeme",
          "Kimlik dogrulama sistemleri, gelismis sepet ozellikleri",
          "Veritabani mimarisi ve sorgu optimizasyonu"
        ]
      },
      "gesk": {
        "company": "GESK Technologies",
        "role": "Stajyer, Gomulu Yazilim Gelistirici",
        "period": "Eyl 2023 - Eki 2023",
        "location": "Istanbul",
        "bullets": [
          "STM32 uzerinde Flappy Bird oyunu (C/C++)",
          "Gercek zamanli programlama, donanim-yazilim entegrasyonu"
        ]
      },
      "kplus": {
        "company": "KPlus Bilgi Teknolojileri",
        "role": "Stajyer, Fullstack Developer",
        "period": "Tem 2023 - Agu 2023",
        "location": "Istanbul",
        "bullets": [
          "UBER benzeri arac paylasim uygulamasi (.NET)",
          "Gercek zamanli ozellikler, konum hizmetleri, RESTful API"
        ]
      }
    }
  },
  "projects": {
    "heading": "Projeler",
    "items": {
      "microservices": {
        "name": "Microservices Java",
        "description": "Spring Boot mikroservisler, olceklenebilir dagitik mimari. Servis kesfetme, API gateway, event-driven iletisim.",
        "shortDescription": "Servis kesfetme ve event-driven iletisimli Spring Boot mikroservisler"
      },
      "flappybird": {
        "name": "STM32 Flappy Bird",
        "description": "STM32F429I-DISC0 uzerinde interaktif Flappy Bird. TouchGFX grafikleri, gercek zamanli oyun dongusu.",
        "shortDescription": "TouchGFX ile STM32 uzerinde gercek zamanli Flappy Bird"
      },
      "behaviorwatch": {
        "name": "BehaviorWatch AI",
        "description": "YOLOv8 ve OpenCV ile gercek zamanli sigara tespit sistemi.",
        "shortDescription": "YOLOv8 ve OpenCV ile gercek zamanli sigara tespiti"
      },
      "bunqchat": {
        "name": "Bunq Chat Backend",
        "description": "Bunq icin gercek zamanli sohbet uygulamasi, temiz mimari ve RESTful API tasarimi.",
        "shortDescription": "Temiz mimari ile gercek zamanli sohbet backend"
      },
      "allinpro": {
        "name": "AllInPro E-Ticaret",
        "description": "Odeme entegrasyonlu, urun yonetimi ve siparis islemeli tam donanimli e-ticaret platformu.",
        "shortDescription": "Odeme entegrasyonlu tam donanimli e-ticaret platformu"
      },
      "cansat": {
        "name": "CanSat Yer Istasyonu",
        "description": "NASA/Lockheed Martin koordinasyonunda CanSat yarismasi yer istasyonu. Gercek zamanli telemetri, GPS takip, irtifa izleme.",
        "shortDescription": "Gercek zamanli telemetri ve GPS takipli CanSat yer istasyonu"
      }
    }
  },
  "achievements": {
    "heading": "Basarilar",
    "items": {
      "teknofest": {
        "name": "TEKNOFEST 2021 ve 2022",
        "subtitle": "Finalist",
        "description": "Goruntu isleme, IHA otonom ucus ve roket yarismasinda yer istasyonu tasarimi"
      },
      "cansat": {
        "name": "CanSat Yarismasi",
        "subtitle": "Finalist",
        "description": "ST FOCOUNT takimi yer istasyonu yonetimi, NASA ve Lockheed Martin koordinasyonlu"
      }
    }
  },
  "contact": {
    "heading": "Iletisim",
    "email": "E-posta",
    "github": "GitHub",
    "linkedin": "LinkedIn"
  }
}
```

IMPORTANT: Remove the old `greeting` key from hero in both files (it is replaced by the new structure). Ensure en.json and tr.json have EXACTLY the same key paths. After writing both files, do a quick diff of the key structures.
  </action>
  <verify>
    <automated>node -e "const en=require('./src/messages/en.json');const tr=require('./src/messages/tr.json');const ek=JSON.stringify(Object.keys(en).sort());const tk=JSON.stringify(Object.keys(tr).sort());if(ek!==tk){console.error('Top-level key mismatch');process.exit(1);}console.log('i18n parity OK');const d=require('./src/lib/data.ts'?'skip':'skip');console.log('Files exist');" && node -e "const fs=require('fs');const f=fs.readFileSync('src/app/fonts.ts','utf8');if(!f.includes('\\u9580\\u9053\\u6280')){console.error('Font text param missing');process.exit(1);}console.log('Font fix OK');"</automated>
  </verify>
  <acceptance_criteria>
    - src/app/fonts.ts contains `text: "\u9580\u9053\u6280\u6226\u4F5C\u8A89\u7D50"` in notoSerifJP config
    - src/lib/data.ts exports SKILLS_DATA, EXPERIENCE_DATA, PROJECTS_DATA, ACHIEVEMENTS_DATA, CONTACT_LINKS, STATS_DATA
    - src/messages/en.json contains keys: hero.downloadCV, about.heading, about.bio, about.stats.experience, about.education.degree, skills.heading, skills.categories.languages, skills.items.java, experience.heading, experience.positions.seyfhnova.company, projects.heading, projects.items.microservices.name, achievements.heading, achievements.items.teknofest.name, contact.heading
    - src/messages/tr.json contains identical key paths as en.json
    - hero.greeting key is REMOVED from both en.json and tr.json
    - en.json hero.role is "Full-Stack Developer" (not "Mid-Level Backend Developer")
    - tr.json hero.tagline is "Saglam sistemleri hassasiyet ve amacla insa etmek"
  </acceptance_criteria>
  <done>All i18n content for 7 sections in both locales with structural parity, typed data module with portfolio structural data, Noto Serif JP font micro-subsetting for 7 kanji characters</done>
</task>

<task type="auto">
  <name>Task 2: Shared UI components (ParchmentCard, ProficiencyBar, TechTag, MedalBadge, MonIcon)</name>
  <files>
    src/components/ui/ParchmentCard.tsx,
    src/components/ui/ProficiencyBar.tsx,
    src/components/ui/TechTag.tsx,
    src/components/ui/MedalBadge.tsx,
    src/components/ui/MonIcon.tsx
  </files>
  <read_first>
    src/lib/cn.ts,
    src/app/globals.css,
    .planning/phases/02-content-and-visual-design/02-UI-SPEC.md,
    .planning/phases/02-content-and-visual-design/02-RESEARCH.md
  </read_first>
  <action>
Create 5 shared UI components under `src/components/ui/`. All are Server Components (no "use client"). All use `cn()` from `@/lib/cn` for conditional classes.

**1. ParchmentCard.tsx:**

```typescript
import { cn } from "@/lib/cn";

interface ParchmentCardProps {
  children: React.ReactNode;
  className?: string;
}

export function ParchmentCard({ children, className }: ParchmentCardProps) {
  return (
    <div
      className={cn(
        "relative bg-surface rounded-lg p-6",
        "border border-accent-gold/15",
        "shadow-[inset_0_2px_4px_rgba(0,0,0,0.3),inset_0_-2px_4px_rgba(0,0,0,0.2)]",
        className
      )}
    >
      {/* Paper grain overlay */}
      <div
        className="absolute inset-0 rounded-lg pointer-events-none opacity-5"
        style={{
          backgroundImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.5'/%3E%3C/svg%3E")`,
        }}
      />
      <div className="relative">{children}</div>
    </div>
  );
}
```

- Surface: `bg-surface` (#1a1a1a)
- Border: `border-accent-gold/15` (gold at 15% opacity)
- Border-radius: `rounded-lg` (8px)
- Padding: `p-6` (24px)
- Rolled-edge effect: `box-shadow: inset 0 2px 4px rgba(0,0,0,0.3), inset 0 -2px 4px rgba(0,0,0,0.2)`
- Paper grain: inline SVG feTurbulence at 5% opacity as pseudo-overlay

**2. ProficiencyBar.tsx:**

```typescript
interface ProficiencyBarProps {
  label: string;
  percentage: number;
}

export function ProficiencyBar({ label, percentage }: ProficiencyBarProps) {
  return (
    <div className="flex flex-col gap-1">
      <div className="flex justify-between items-center">
        <span className="text-body text-text-secondary">{label}</span>
        <span className="text-body text-text-secondary text-sm">{percentage}%</span>
      </div>
      <div className="h-1.5 bg-bg rounded-sm overflow-hidden">
        <div
          className="h-full bg-accent-red rounded-sm"
          style={{ width: `${percentage}%` }}
        />
      </div>
    </div>
  );
}
```

- Bar height: `h-1.5` (6px)
- Track: `bg-bg` (#0a0a0a, darker than card surface)
- Fill: `bg-accent-red` (#c0392b)
- Border-radius: `rounded-sm` (3px)
- Label and percentage: `text-body text-text-secondary`

**3. TechTag.tsx:**

```typescript
export function TechTag({ label }: { label: string }) {
  return (
    <span className="inline-block text-body text-text-secondary bg-accent-red/10 border border-accent-red/20 rounded px-2 py-1 text-sm">
      {label}
    </span>
  );
}
```

- Background: `bg-accent-red/10` (red at 10% opacity)
- Border: `border-accent-red/20` (red at 20% opacity)
- Border-radius: `rounded` (4px)
- Padding: `px-2 py-1` (8px x 4px)
- Text: `text-sm text-text-secondary`

**4. MedalBadge.tsx:**

```typescript
import { cn } from "@/lib/cn";

interface MedalBadgeProps {
  icon?: React.ReactNode;
  name: string;
  subtitle: string;
  description: string;
  className?: string;
}

export function MedalBadge({ icon, name, subtitle, description, className }: MedalBadgeProps) {
  return (
    <div className={cn("flex flex-col items-center text-center", className)}>
      {/* Medal circle */}
      <div className="w-[120px] h-[120px] rounded-full border-3 border-accent-gold flex items-center justify-center mb-6 shadow-[0_0_15px_rgba(212,165,116,0.2),inset_0_0_15px_rgba(212,165,116,0.1)]">
        {icon || (
          <span className="font-heading text-h2 text-accent-gold">{subtitle.charAt(0)}</span>
        )}
      </div>
      {/* Card below */}
      <div className="bg-bg rounded-lg p-6 w-full">
        <h3 className="font-heading text-h3 text-paper font-bold mb-1">{name}</h3>
        <p className="text-body text-accent-gold mb-3">{subtitle}</p>
        <p className="text-body text-text-secondary leading-relaxed">{description}</p>
      </div>
    </div>
  );
}
```

- Medal circle: 120px diameter, `border-3 border-accent-gold`, metallic glow via `box-shadow: 0 0 15px rgba(212, 165, 116, 0.2), inset 0 0 15px rgba(212, 165, 116, 0.1)`
- Card: `bg-bg` surface, padding 24px, rounded-lg
- Name: `text-h3`, `text-paper`, bold
- Subtitle: `text-body`, `text-accent-gold`
- Description: `text-body`, `text-text-secondary`

**5. MonIcon.tsx:**

```typescript
import { cn } from "@/lib/cn";

interface MonIconProps {
  href: string;
  label: string;
  children: React.ReactNode; // SVG icon
  className?: string;
}

export function MonIcon({ href, label, children, className }: MonIconProps) {
  return (
    <a
      href={href}
      target="_blank"
      rel="noopener noreferrer"
      className={cn(
        "group flex flex-col items-center gap-3",
        className
      )}
    >
      <div className="w-16 h-16 rounded-full border-2 border-accent-gold flex items-center justify-center transition-all duration-normal group-hover:border-accent-red group-hover:shadow-[0_0_20px_rgba(192,57,43,0.3)]">
        <span className="text-paper w-6 h-6">{children}</span>
      </div>
      <span className="text-body text-text-secondary group-hover:text-paper transition-colors duration-normal">
        {label}
      </span>
    </a>
  );
}
```

- Container: 64px circle (`w-16 h-16`), `border-2 border-accent-gold`, `rounded-full`
- Hover: `border-accent-red`, `box-shadow: 0 0 20px rgba(192, 57, 43, 0.3)`, transition 300ms
- Icon: `text-paper`, 24px (`w-6 h-6`)
- Label below: `text-body text-text-secondary`, hover `text-paper`
- Link: `target="_blank" rel="noopener noreferrer"`
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ui/ParchmentCard.tsx exists and exports ParchmentCard
    - src/components/ui/ParchmentCard.tsx contains `border-accent-gold/15` and `shadow-[inset_0_2px_4px`
    - src/components/ui/ParchmentCard.tsx contains `feTurbulence` (paper grain SVG)
    - src/components/ui/ProficiencyBar.tsx exists and exports ProficiencyBar
    - src/components/ui/ProficiencyBar.tsx contains `bg-accent-red` and `style={{ width:`
    - src/components/ui/TechTag.tsx exists and exports TechTag
    - src/components/ui/TechTag.tsx contains `bg-accent-red/10` and `border-accent-red/20`
    - src/components/ui/MedalBadge.tsx exists and exports MedalBadge
    - src/components/ui/MedalBadge.tsx contains `border-accent-gold` and `120px`
    - src/components/ui/MonIcon.tsx exists and exports MonIcon
    - src/components/ui/MonIcon.tsx contains `w-16 h-16` and `group-hover:border-accent-red`
    - None of the 5 files contain "use client"
    - TypeScript compilation produces no errors for these files
  </acceptance_criteria>
  <done>5 shared UI components created as Server Components — ParchmentCard with paper grain, ProficiencyBar with katana-style red fill, TechTag with ink-stamp styling, MedalBadge with gold gradient medal circle, MonIcon with mon crest hover glow</done>
</task>

<task type="auto">
  <name>Task 3: SectionDivider, page.tsx rewire, and paper grain global CSS</name>
  <files>
    src/components/layout/SectionDivider.tsx,
    src/app/[locale]/page.tsx,
    src/app/globals.css
  </files>
  <read_first>
    src/app/[locale]/page.tsx,
    src/components/layout/SectionShell.tsx,
    src/app/globals.css,
    src/lib/constants.ts,
    .planning/phases/02-content-and-visual-design/02-UI-SPEC.md
  </read_first>
  <action>
**1. Create SectionDivider.tsx:**

```typescript
export function SectionDivider() {
  return (
    <div className="relative w-full h-8 overflow-hidden">
      {/* Katana slash line */}
      <div
        className="absolute top-1/2 left-[-5%] w-[110%] h-[2px] -translate-y-1/2 -rotate-2"
        style={{ background: "rgba(192, 57, 43, 0.25)" }}
      />
      {/* Asanoha geometric pattern strip */}
      <div
        className="absolute inset-0 opacity-[0.06] pointer-events-none"
        style={{
          backgroundImage: `url("data:image/svg+xml,%3Csvg width='24' height='24' viewBox='0 0 24 24' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M12 0L24 12L12 24L0 12Z' fill='none' stroke='%23d4a574' stroke-width='0.5'/%3E%3Cpath d='M12 0L12 24M0 12L24 12' stroke='%23d4a574' stroke-width='0.3'/%3E%3C/svg%3E")`,
          backgroundSize: "24px 24px",
        }}
      />
    </div>
  );
}
```

- Katana slash: 2px line, `rgba(192, 57, 43, 0.25)` (accent-red at 25% opacity), rotated -2deg, extends 5% beyond container on each side
- Asanoha pattern: inline SVG diamond pattern with gold stroke at 6% opacity, used as repeating background
- Container: `h-8` (32px), `overflow-hidden` to clip the extended slash

**2. Rewrite page.tsx to wire section components and alternating backgrounds:**

Replace the entire page content. Import section components (they don't exist yet — import them by name, they will be created in Plans 02 and 03). For now, create placeholder server components inline so the build doesn't break.

```typescript
import { setRequestLocale } from "next-intl/server";
import { SECTION_IDS } from "@/lib/constants";
import type { SectionId } from "@/lib/constants";
import { SectionShell } from "@/components/layout/SectionShell";
import { SectionDivider } from "@/components/layout/SectionDivider";

// Placeholder components — replaced by real sections in Plans 02-02 and 02-03
function PlaceholderSection({ id }: { id: string }) {
  return (
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-center py-20">
      <h2 className="font-heading text-h2 text-text-primary capitalize">{id}</h2>
    </div>
  );
}

const SECTION_BG: Record<SectionId, string> = {
  hero: "bg-bg",
  about: "bg-surface",
  skills: "bg-bg",
  experience: "bg-surface",
  projects: "bg-bg",
  achievements: "bg-surface",
  contact: "bg-bg",
};

export default async function HomePage({
  params,
}: {
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  setRequestLocale(locale);

  return (
    <main>
      {SECTION_IDS.map((id, index) => (
        <div key={id}>
          <SectionShell
            id={id}
            className={`${SECTION_BG[id]} ${id === "hero" ? "min-h-[90vh]" : ""}`}
          >
            <PlaceholderSection id={id} />
          </SectionShell>
          {index < SECTION_IDS.length - 1 && <SectionDivider />}
        </div>
      ))}
    </main>
  );
}
```

Key changes from current page.tsx:
- Added `SECTION_BG` map for alternating backgrounds (odd: `bg-bg`, even: `bg-surface`)
- Pass background class to SectionShell via className
- SectionDivider rendered between every pair of sections (not after last)
- Placeholder components until Plans 02/03 create real ones
- Removed `min-h-screen` from placeholder inner div (sections don't need to be full-screen, only hero does via `min-h-[90vh]`)

**3. Add paper-grain utility class to globals.css:**

Add after the existing `@utility z-modal` block:

```css
@utility paper-grain {
  position: relative;
}
```

Note: Paper grain is handled directly in ParchmentCard via inline SVG. The utility is a lightweight marker class. The actual texture implementation uses inline SVG in the component for maximum portability.

Also verify the existing `@media (prefers-reduced-motion: reduce)` block is present and handles all transitions. It already is — no changes needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/SectionDivider.tsx exists and exports SectionDivider
    - src/components/layout/SectionDivider.tsx contains `rotate(-2deg)` or `-rotate-2` (katana slash angle)
    - src/components/layout/SectionDivider.tsx contains `rgba(192, 57, 43, 0.25)` (red at 25% opacity)
    - src/components/layout/SectionDivider.tsx contains `asanoha` or diamond SVG pattern with `%23d4a574` (gold color)
    - src/app/[locale]/page.tsx imports SectionDivider
    - src/app/[locale]/page.tsx contains `bg-bg` and `bg-surface` (alternating backgrounds)
    - src/app/[locale]/page.tsx renders SectionDivider between sections with `index < SECTION_IDS.length - 1` guard
    - `npx next build` exits 0 with /en and /tr routes
  </acceptance_criteria>
  <done>SectionDivider with katana slash and asanoha pattern, page.tsx with alternating section backgrounds and dividers between sections, build succeeds</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds with zero errors
2. Both en.json and tr.json have identical key structures (all 7 sections + hero + nav + meta)
3. Noto Serif JP font config includes `text` parameter for kanji micro-subset
4. All 5 UI components type-check cleanly
5. SectionDivider renders between sections with katana slash visual
6. Alternating backgrounds visible (odd #0a0a0a, even #1a1a1a)
</verification>

<success_criteria>
- Build succeeds with all new files
- i18n parity between en.json and tr.json (identical key paths)
- Font fix enables proper kanji rendering
- 5 UI components exported and type-safe
- SectionDivider with katana slash and geometric pattern
- Page shows alternating section backgrounds
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-and-visual-design/02-01-SUMMARY.md`
</output>
