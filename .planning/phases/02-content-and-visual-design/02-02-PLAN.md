---
phase: 02-content-and-visual-design
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/sections/Hero.tsx
  - src/components/sections/About.tsx
  - src/components/sections/Skills.tsx
  - src/app/[locale]/page.tsx
  - public/Emirhan_Kaya_CV.pdf
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-08, VISL-02]
user_setup:
  - service: local-files
    why: "Profile photo and CV PDF needed"
    env_vars: []
    dashboard_config:
      - task: "Place profile photo at public/images/emirhan-3.jpg"
        location: "Local filesystem"
      - task: "Place CV PDF at public/Emirhan_Kaya_CV.pdf"
        location: "Local filesystem"

must_haves:
  truths:
    - "User sees hero with large name, role subtitle, tagline, and red Download CV button"
    - "User sees About section with profile photo in ink-wash frame, bio text, 3 stat counters, and education"
    - "User sees Skills section with 4 parchment cards containing proficiency bars"
    - "User can click Download CV to get a PDF file"
    - "Content displays correctly in both Turkish and English"
  artifacts:
    - path: "src/components/sections/Hero.tsx"
      provides: "Full-screen hero with centered name, role, tagline, CTA, scroll indicator"
      exports: ["Hero"]
    - path: "src/components/sections/About.tsx"
      provides: "About section with photo, bio, stats, education"
      exports: ["About"]
    - path: "src/components/sections/Skills.tsx"
      provides: "Skills section with 4 parchment cards and proficiency bars"
      exports: ["Skills"]
  key_links:
    - from: "src/components/sections/Hero.tsx"
      to: "src/messages/en.json"
      via: "getTranslations('hero')"
      pattern: "getTranslations.*hero"
    - from: "src/components/sections/About.tsx"
      to: "src/lib/data.ts"
      via: "import STATS_DATA"
      pattern: "import.*STATS_DATA.*from"
    - from: "src/components/sections/Skills.tsx"
      to: "src/components/ui/ParchmentCard.tsx"
      via: "import ParchmentCard"
      pattern: "import.*ParchmentCard"
    - from: "src/components/sections/Skills.tsx"
      to: "src/components/ui/ProficiencyBar.tsx"
      via: "import ProficiencyBar"
      pattern: "import.*ProficiencyBar"
---

<objective>
Build the top three sections of the portfolio — Hero, About, and Skills — and wire them into page.tsx, replacing the placeholder components from Plan 01.

Purpose: These are the first sections a recruiter sees. Hero creates the dramatic first impression, About establishes credibility, and Skills shows technical depth. Together they cover the top-of-fold experience.

Output: Three async Server Components using getTranslations, the CV PDF placeholder, and page.tsx updated to render real section components for hero, about, and skills.
</objective>

<execution_context>
@.planning/phases/02-content-and-visual-design/02-CONTEXT.md
@.planning/phases/02-content-and-visual-design/02-RESEARCH.md
@.planning/phases/02-content-and-visual-design/02-UI-SPEC.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@EMIRHAN_KAYA_DATA.md
@.planning/phases/02-content-and-visual-design/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/data.ts:
```typescript
export const SKILLS_DATA: {
  languages: Array<{ key: string; proficiency: number }>;
  frameworks: Array<{ key: string; proficiency: number }>;
  infrastructure: Array<{ key: string; proficiency: number }>;
  security: Array<{ key: string; proficiency: number }>;
};
export type SkillCategory = "languages" | "frameworks" | "infrastructure" | "security";
export const STATS_DATA: Array<{ key: string; value: string }>;
```

From src/messages/en.json (relevant keys):
```json
{
  "hero": { "name", "role", "tagline", "downloadCV", "scrollDown" },
  "about": { "heading", "bio", "photoAlt", "stats": { "experience", "projects", "companies" }, "education": { "degree", "university", "period", "gpa" } },
  "skills": { "heading", "categories": { "languages", "frameworks", "infrastructure", "security" }, "items": { "java", "kotlin", ... } }
}
```

From src/components/ui/ParchmentCard.tsx:
```typescript
export function ParchmentCard({ children, className }: { children: React.ReactNode; className?: string }): JSX.Element;
```

From src/components/ui/ProficiencyBar.tsx:
```typescript
export function ProficiencyBar({ label, percentage }: { label: string; percentage: number }): JSX.Element;
```

From src/components/layout/SectionShell.tsx:
```typescript
export function SectionShell({ id, children, className, showKanji }: SectionShellProps): JSX.Element;
```

From src/lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero section with ink-wash background, CTA button, and scroll indicator</name>
  <files>
    src/components/sections/Hero.tsx,
    public/Emirhan_Kaya_CV.pdf
  </files>
  <read_first>
    src/messages/en.json,
    src/messages/tr.json,
    src/app/globals.css,
    src/components/layout/SectionShell.tsx,
    .planning/phases/02-content-and-visual-design/02-UI-SPEC.md
  </read_first>
  <action>
**1. Create src/components/sections/Hero.tsx as an async Server Component:**

```typescript
import { getTranslations } from "next-intl/server";

export async function Hero() {
  const t = await getTranslations("hero");

  return (
    <div className="relative flex flex-col items-center justify-center min-h-[90vh] text-center px-4">
      {/* Sumi-e ink splash background */}
      <div
        className="absolute inset-0 pointer-events-none"
        style={{
          background: `radial-gradient(ellipse at center, rgba(192, 57, 43, 0.03) 0%, rgba(30, 30, 30, 0.05) 30%, transparent 70%)`,
        }}
        aria-hidden="true"
      />

      {/* Content */}
      <div className="relative z-base">
        <h1
          className="font-heading font-bold text-paper leading-[1.1]"
          style={{ fontSize: "clamp(3rem, 5vw, 6rem)" }}
        >
          {t("name")}
        </h1>
        <p className="text-h3 text-text-secondary opacity-80 mt-4">
          {t("role")}
        </p>
        <p className="text-body text-text-secondary italic mt-3 max-w-[600px] mx-auto">
          {t("tagline")}
        </p>
        <a
          href="/Emirhan_Kaya_CV.pdf"
          download
          className="inline-block mt-8 bg-accent-red text-paper px-6 py-2 rounded-md font-medium transition-all duration-normal hover:bg-accent-red-bright hover:shadow-[0_0_20px_rgba(192,57,43,0.4)]"
        >
          {t("downloadCV")}
        </a>
      </div>

      {/* Scroll indicator — CSS bounce animation */}
      <div className="absolute bottom-8 left-1/2 -translate-x-1/2 animate-bounce">
        <svg
          width="24"
          height="24"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          strokeWidth="2"
          className="text-text-secondary opacity-60"
          aria-hidden="true"
        >
          <polyline points="6 9 12 15 18 9" />
        </svg>
      </div>
    </div>
  );
}
```

Key specs from UI-SPEC:
- Name: Playfair Display (`font-heading`), `clamp(3rem, 5vw, 6rem)`, `text-paper`, `font-bold`, `line-height: 1.1`
- Role: `text-h3` (1.5rem/24px), `text-text-secondary`, `opacity-80`
- Tagline: `text-body` (16px), `text-text-secondary`, italic
- CTA button: `bg-accent-red text-paper`, padding `py-2 px-6`, `rounded-md` (6px), hover `bg-accent-red-bright` with `box-shadow: 0 0 20px rgba(192, 57, 43, 0.4)`
- Ink splash background: radial gradient from center, red at 3% opacity fading through dark gray to transparent
- Scroll indicator: CSS `animate-bounce` on chevron SVG, positioned bottom-center
- NO "use client" — this is a Server Component using `getTranslations`
- NO profile photo in hero (locked decision)

**2. Create a placeholder CV PDF:**

Create a minimal text file at `public/Emirhan_Kaya_CV.pdf` as a placeholder. The user will replace this with their actual PDF. Content: just an empty file or a note. Use `Write` to create an empty file at this path — the user will replace it.

Note: If the public directory doesn't exist, create it first.

**3. Update Navbar.tsx CV download href:**

Read `src/components/layout/Navbar.tsx`. Change all occurrences of `href="/cv.pdf"` to `href="/Emirhan_Kaya_CV.pdf"`. There are 3 occurrences:
- Line 112: desktop CV download button
- Line 199: mobile menu CV download (animated version)
- Line 231: mobile menu CV download (reduced motion version)

Also update the Navbar to use i18n for the CV button text. Currently it says hardcoded "Download CV". Change to use `useTranslations("nav")` which the Navbar may already import. Replace the hardcoded "Download CV" text with `t("downloadCV")` where `t` comes from `useTranslations("nav")`. Check if Navbar already uses useTranslations — if not, add the import and hook call.

Similarly, update NAV_LINKS label rendering in Navbar to use i18n. Currently `{link.label}` uses the hardcoded English label from constants.ts. Change to `{t(link.id)}` using the nav namespace (en.json already has `nav.about`, `nav.skills`, etc.).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/Hero.tsx exists and exports async function Hero
    - Hero.tsx does NOT contain "use client"
    - Hero.tsx contains `getTranslations("hero")`
    - Hero.tsx contains `clamp(3rem, 5vw, 6rem)` for name font size
    - Hero.tsx contains `href="/Emirhan_Kaya_CV.pdf"` with `download` attribute
    - Hero.tsx contains `radial-gradient(ellipse at center, rgba(192, 57, 43, 0.03)` for ink splash
    - Hero.tsx contains `animate-bounce` for scroll indicator
    - Hero.tsx contains `font-heading font-bold text-paper` on the h1
    - public/Emirhan_Kaya_CV.pdf exists (placeholder)
    - src/components/layout/Navbar.tsx contains `href="/Emirhan_Kaya_CV.pdf"` (not `/cv.pdf`)
    - src/components/layout/Navbar.tsx uses `useTranslations("nav")` for link labels
  </acceptance_criteria>
  <done>Hero section renders centered name (Playfair Display clamp), role, tagline, red CTA for CV download, ink-splash background, and bouncing scroll indicator. Navbar CV links updated to correct filename and i18n labels.</done>
</task>

<task type="auto">
  <name>Task 2: About section with ink-wash photo frame, stats, and education</name>
  <files>
    src/components/sections/About.tsx
  </files>
  <read_first>
    src/messages/en.json,
    src/lib/data.ts,
    src/components/ui/ParchmentCard.tsx,
    .planning/phases/02-content-and-visual-design/02-UI-SPEC.md
  </read_first>
  <action>
Create `src/components/sections/About.tsx` as an async Server Component:

```typescript
import { getTranslations } from "next-intl/server";
import Image from "next/image";
import { STATS_DATA } from "@/lib/data";

export async function About() {
  const t = await getTranslations("about");

  return (
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      {/* Section heading */}
      <h2 className="font-heading text-h2 text-paper text-center mb-12 font-bold">
        {t("heading")}
      </h2>

      {/* Photo + Bio grid */}
      <div className="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
        {/* Profile photo with ink-wash frame */}
        <div className="lg:col-span-5 flex justify-center">
          <div className="relative w-[200px] h-[200px]">
            <Image
              src="/images/emirhan-3.jpg"
              alt={t("photoAlt")}
              fill
              className="object-cover"
              sizes="200px"
              style={{
                maskImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M100 8c25 -2 48 8 65 25s28 40 27 65c-1 25 -12 48 -30 63s-42 23 -65 22c-23 -1 -45 -10 -60 -27S12 116 13 93c1 -23 13 -45 32 -60S75 10 100 8z' fill='black'/%3E%3C/svg%3E")`,
                maskSize: "cover",
                maskRepeat: "no-repeat",
                WebkitMaskImage: `url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M100 8c25 -2 48 8 65 25s28 40 27 65c-1 25 -12 48 -30 63s-42 23 -65 22c-23 -1 -45 -10 -60 -27S12 116 13 93c1 -23 13 -45 32 -60S75 10 100 8z' fill='black'/%3E%3C/svg%3E")`,
                WebkitMaskSize: "cover",
                WebkitMaskRepeat: "no-repeat",
              }}
            />
            {/* Ink-wash ring decoration */}
            <div
              className="absolute inset-[-8px] rounded-full border-2 border-accent-gold/20 pointer-events-none"
              style={{
                borderRadius: "48% 52% 45% 55% / 52% 48% 52% 48%",
              }}
              aria-hidden="true"
            />
          </div>
        </div>

        {/* Bio + Education */}
        <div className="lg:col-span-7">
          <p className="text-body text-paper leading-relaxed max-w-[600px]">
            {t("bio")}
          </p>
          {/* Education */}
          <div className="mt-4 text-body text-text-secondary">
            <p>{t("education.degree")} — {t("education.university")}</p>
            <p>{t("education.period")} | {t("education.gpa")}</p>
          </div>
        </div>
      </div>

      {/* Stat counters */}
      <div className="grid grid-cols-3 gap-8 mt-12 max-w-lg mx-auto text-center">
        {STATS_DATA.map((stat) => (
          <div key={stat.key}>
            <span className="block font-heading text-h2 text-accent-red font-bold">
              {stat.value}
            </span>
            <span className="text-body text-text-secondary">
              {t(`stats.${stat.key}`)}
            </span>
          </div>
        ))}
      </div>
    </div>
  );
}
```

Key specs from UI-SPEC:
- Layout: photo left (5 cols) + text right (7 cols) on desktop via `grid-cols-12`, stacked on mobile
- Photo: 200px container, CSS mask with organic brush-stroke SVG path (irregular blob shape, not perfect circle)
- Ink-wash frame: outer ring with `border-accent-gold/20` and organic `border-radius` values (48% 52% etc.) for imperfect feel
- Bio: `text-body text-paper`, max-width 600px, `leading-relaxed` (1.6 line-height)
- Education: `text-body text-text-secondary`, below bio
- Stats: 3 columns centered, number in `text-h2 text-accent-red font-bold` (heading weight 700), label in `text-body text-text-secondary`
- Stats values from STATS_DATA ("1+", "39+", "4"), labels from i18n t(`stats.${key}`)
- NO "use client" — Server Component with getTranslations
- Uses Next.js Image component with `fill`, `sizes="200px"`, no `priority` (below fold)

The SVG mask path creates an organic irregular blob that feels like a brush-stroke circle rather than a perfect geometric circle. This satisfies the "organic and imperfect" requirement from CONTEXT.md specifics.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/About.tsx exists and exports async function About
    - About.tsx does NOT contain "use client"
    - About.tsx contains `getTranslations("about")`
    - About.tsx contains `Image` import from "next/image"
    - About.tsx contains `src="/images/emirhan-3.jpg"`
    - About.tsx contains `maskImage` with SVG data URI (brush-stroke mask)
    - About.tsx contains `STATS_DATA` import from "@/lib/data"
    - About.tsx contains `text-accent-red` on stat numbers
    - About.tsx contains `grid-cols-1 lg:grid-cols-12` for responsive layout
    - About.tsx contains `t("education.degree")` for education rendering
  </acceptance_criteria>
  <done>About section renders profile photo with organic ink-wash SVG mask frame, bio text, education info, and 3 stat counters (static numbers) with i18n labels in both locales</done>
</task>

<task type="auto">
  <name>Task 3: Skills section with 4 parchment cards and proficiency bars, and wire Hero/About/Skills into page.tsx</name>
  <files>
    src/components/sections/Skills.tsx,
    src/app/[locale]/page.tsx
  </files>
  <read_first>
    src/lib/data.ts,
    src/messages/en.json,
    src/components/ui/ParchmentCard.tsx,
    src/components/ui/ProficiencyBar.tsx,
    src/app/[locale]/page.tsx,
    .planning/phases/02-content-and-visual-design/02-UI-SPEC.md
  </read_first>
  <action>
**1. Create src/components/sections/Skills.tsx as an async Server Component:**

```typescript
import { getTranslations } from "next-intl/server";
import { SKILLS_DATA, type SkillCategory } from "@/lib/data";
import { ParchmentCard } from "@/components/ui/ParchmentCard";
import { ProficiencyBar } from "@/components/ui/ProficiencyBar";

const CATEGORY_ORDER: SkillCategory[] = ["languages", "frameworks", "infrastructure", "security"];

export async function Skills() {
  const t = await getTranslations("skills");

  return (
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <h2 className="font-heading text-h2 text-paper text-center mb-12 font-bold">
        {t("heading")}
      </h2>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {CATEGORY_ORDER.map((category) => (
          <ParchmentCard key={category}>
            <h3 className="font-heading text-h3 text-paper font-bold mb-6">
              {t(`categories.${category}`)}
            </h3>
            <div className="flex flex-col gap-4">
              {SKILLS_DATA[category].map((skill) => (
                <ProficiencyBar
                  key={skill.key}
                  label={t(`items.${skill.key}`)}
                  percentage={skill.proficiency}
                />
              ))}
            </div>
          </ParchmentCard>
        ))}
      </div>
    </div>
  );
}
```

Key specs:
- Layout: 2-column grid on desktop (`lg:grid-cols-2`), 1-column on mobile
- 4 parchment cards, one per category: Languages, Frameworks, Infrastructure & DevOps, Messaging & Security
- Card title: `text-h3 text-paper font-bold` (1.5rem/24px, weight 700)
- Each card contains ProficiencyBar for each skill in that category
- Skill labels come from i18n `t("items.${key}")`, proficiency from SKILLS_DATA
- Gap between cards: `gap-8` (32px)
- NO "use client"

**2. Update page.tsx to render Hero, About, Skills (replace placeholders):**

Read current page.tsx (from Plan 01 output). Import the three new section components and replace the PlaceholderSection for hero, about, and skills:

```typescript
import { setRequestLocale } from "next-intl/server";
import { SECTION_IDS } from "@/lib/constants";
import type { SectionId } from "@/lib/constants";
import { SectionShell } from "@/components/layout/SectionShell";
import { SectionDivider } from "@/components/layout/SectionDivider";
import { Hero } from "@/components/sections/Hero";
import { About } from "@/components/sections/About";
import { Skills } from "@/components/sections/Skills";

// Placeholder for sections not yet implemented (Plan 02-03 replaces these)
function PlaceholderSection({ id }: { id: string }) {
  return (
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 flex items-center justify-center py-20">
      <h2 className="font-heading text-h2 text-text-primary capitalize">{id}</h2>
    </div>
  );
}

const SECTION_BG: Record<SectionId, string> = {
  hero: "bg-bg",
  about: "bg-surface",
  skills: "bg-bg",
  experience: "bg-surface",
  projects: "bg-bg",
  achievements: "bg-surface",
  contact: "bg-bg",
};

const SECTION_COMPONENTS: Partial<Record<SectionId, React.ComponentType>> = {
  hero: Hero,
  about: About,
  skills: Skills,
};

export default async function HomePage({
  params,
}: {
  params: Promise<{ locale: string }>;
}) {
  const { locale } = await params;
  setRequestLocale(locale);

  return (
    <main>
      {SECTION_IDS.map((id, index) => {
        const Component = SECTION_COMPONENTS[id];
        return (
          <div key={id}>
            <SectionShell
              id={id}
              className={`${SECTION_BG[id]} ${id === "hero" ? "min-h-[90vh]" : ""}`}
            >
              {Component ? <Component /> : <PlaceholderSection id={id} />}
            </SectionShell>
            {index < SECTION_IDS.length - 1 && <SectionDivider />}
          </div>
        );
      })}
    </main>
  );
}
```

Note: The SECTION_COMPONENTS map allows incremental replacement. Plan 03 will add Experience, Projects, Achievements, Contact to this map.

Type note: Since the section components are async Server Components, the type for SECTION_COMPONENTS may need to be `React.ComponentType<object> | (() => Promise<JSX.Element>)`. Adjust typing as needed to satisfy TypeScript. If async components cause type issues, cast them: `hero: Hero as unknown as React.ComponentType`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/sections/Hero.test.tsx tests/sections/About.test.tsx tests/sections/Skills.test.tsx --reporter=verbose && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/Skills.tsx exists and exports async function Skills
    - Skills.tsx does NOT contain "use client"
    - Skills.tsx contains `getTranslations("skills")`
    - Skills.tsx imports ParchmentCard from "@/components/ui/ParchmentCard"
    - Skills.tsx imports ProficiencyBar from "@/components/ui/ProficiencyBar"
    - Skills.tsx imports SKILLS_DATA from "@/lib/data"
    - Skills.tsx contains `lg:grid-cols-2` for 2-column desktop layout
    - Skills.tsx maps over CATEGORY_ORDER array with 4 categories
    - src/app/[locale]/page.tsx imports Hero, About, Skills from "@/components/sections/"
    - src/app/[locale]/page.tsx renders Hero component for hero section (not placeholder)
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Skills section renders 4 parchment cards with categorized proficiency bars. page.tsx updated to render Hero, About, Skills as real components while keeping placeholders for remaining 4 sections. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx next build` succeeds with zero errors
2. Hero section renders name in Playfair Display with clamp sizing, role, tagline, red CTA button, ink-wash background, and scroll indicator
3. About section renders profile photo with ink-wash mask, bio, education, and 3 stat counters
4. Skills section renders 4 parchment cards with proficiency bars for all skills
5. Navbar CV links point to `/Emirhan_Kaya_CV.pdf` and use i18n for labels
6. All content renders in both EN and TR when switching languages
7. Dev server shows no hydration errors in console
</verification>

<success_criteria>
- Hero, About, Skills sections render correctly as Server Components
- CV download works from hero CTA (links to /Emirhan_Kaya_CV.pdf with download attribute)
- Navbar uses i18n for all labels including Download CV
- Skills section shows 4 categories with proficiency bars inside parchment cards
- Both locales render all content correctly
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-and-visual-design/02-02-SUMMARY.md`
</output>
