---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/globals.css
  - src/app/fonts.ts
  - src/app/[locale]/layout.tsx
  - src/app/[locale]/page.tsx
  - src/middleware.ts
  - src/i18n/routing.ts
  - src/i18n/request.ts
  - src/i18n/navigation.ts
  - src/messages/en.json
  - src/messages/tr.json
  - src/lib/cn.ts
  - src/lib/constants.ts
  - vitest.config.ts
  - tests/theme/design-tokens.test.ts
  - tests/i18n/locale-switch.test.ts
autonomous: true
requirements:
  - FOUND-06
  - FOUND-04
  - FOUND-03

must_haves:
  truths:
    - "Next.js 15 app builds successfully with TypeScript and no errors"
    - "User can visit /en and /tr and see locale-appropriate content"
    - "All design tokens exist as CSS custom properties and generate Tailwind utilities"
    - "Language toggle flag icons switch between TR and EN"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Design tokens via @theme block"
      contains: "--color-bg: #0a0a0a"
    - path: "src/app/[locale]/layout.tsx"
      provides: "Root layout with fonts, NextIntlClientProvider"
      contains: "NextIntlClientProvider"
    - path: "src/middleware.ts"
      provides: "Locale detection and routing"
      contains: "createMiddleware"
    - path: "src/i18n/routing.ts"
      provides: "Locale routing config"
      contains: "defineRouting"
    - path: "src/messages/en.json"
      provides: "English translations"
    - path: "src/messages/tr.json"
      provides: "Turkish translations"
  key_links:
    - from: "src/middleware.ts"
      to: "src/i18n/routing.ts"
      via: "import routing config"
      pattern: "from.*@/i18n/routing"
    - from: "src/app/[locale]/layout.tsx"
      to: "src/messages/*.json"
      via: "getMessages() loads locale JSON"
      pattern: "getMessages"
---

<objective>
Scaffold the Next.js 15 project with TypeScript, Tailwind CSS 4, three-font system, full dark theme design tokens, and complete next-intl i18n routing for TR/EN -- the foundation every subsequent plan depends on.

Purpose: Establishes the core scaffold, design system, and internationalization that all 7 content sections will build upon.
Output: A buildable Next.js 15 app with working locale routing, design tokens generating Tailwind utilities, and placeholder content in both languages.
</objective>

<execution_context>
@C:/Users/babai/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/babai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with all dependencies, fonts, design tokens, and Vitest</name>
  <files>
    package.json, tsconfig.json, next.config.ts, src/app/globals.css, src/app/fonts.ts,
    src/lib/cn.ts, src/lib/constants.ts, vitest.config.ts, tests/theme/design-tokens.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (standard stack versions, @theme pattern, font setup, pitfalls)
    .planning/phases/01-foundation/01-CONTEXT.md (locked color system, typography decisions)
  </read_first>
  <action>
1. Create the Next.js 15 project in the current directory (which already has .planning/ and data files):
   ```bash
   npx create-next-app@15 . --typescript --tailwind --eslint --app --src-dir --turbopack --yes
   ```
   If create-next-app refuses because directory is not empty, initialize manually:
   - Create package.json with next@15, react@19, react-dom@19, typescript, tailwindcss@4, @tailwindcss/postcss
   - Create tsconfig.json with Next.js defaults, paths alias "@/*": ["./src/*"]
   - Create next.config.ts (will be wrapped with next-intl in Task 2)

2. Install all dependencies in exact versions:
   ```bash
   npm install gsap@3 @gsap/react motion lenis next-intl clsx tailwind-merge
   npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom prettier prettier-plugin-tailwindcss @types/react @types/react-dom
   ```

3. Create `src/app/fonts.ts` with three font declarations:
   ```typescript
   import { Inter, Playfair_Display, Noto_Serif_JP } from "next/font/google";

   export const inter = Inter({
     subsets: ["latin"],
     display: "swap",
     variable: "--font-inter",
   });

   export const playfair = Playfair_Display({
     subsets: ["latin"],
     display: "swap",
     variable: "--font-playfair",
   });

   export const notoSerifJP = Noto_Serif_JP({
     weight: ["400", "700"],
     subsets: ["latin"],
     display: "swap",
     variable: "--font-noto-serif-jp",
     preload: false,
   });
   ```

4. Create `src/app/globals.css` with full Tailwind CSS 4 @theme design tokens:
   ```css
   @import "tailwindcss";

   @theme {
     --color-bg: #0a0a0a;
     --color-surface: #1a1a1a;
     --color-accent-red: #c0392b;
     --color-accent-red-bright: #e74c3c;
     --color-accent-gold: #d4a574;
     --color-paper: #f5f0e8;
     --color-bamboo: #2d5016;
     --color-text-primary: #f5f0e8;
     --color-text-secondary: #a0a0a0;

     --font-heading: var(--font-playfair), Georgia, serif;
     --font-kanji: var(--font-noto-serif-jp), serif;
     --font-body: var(--font-inter), system-ui, sans-serif;

     --text-display: 4.5rem;
     --text-h1: 3rem;
     --text-h2: 2.25rem;
     --text-h3: 1.5rem;
     --text-body: 1rem;
     --text-small: 0.875rem;

     --spacing-section: 6rem;
     --spacing-section-mobile: 3rem;

     --duration-fast: 150ms;
     --duration-normal: 300ms;
     --duration-slow: 600ms;
     --duration-extra-slow: 1200ms;

     --ease-smooth: cubic-bezier(0.25, 0.1, 0.25, 1);
     --ease-out-expo: cubic-bezier(0.16, 1, 0.3, 1);

     --z-base: 1;
     --z-section: 10;
     --z-navbar: 100;
     --z-overlay: 200;
     --z-modal: 300;
   }

   @media (prefers-reduced-motion: reduce) {
     *,
     *::before,
     *::after {
       animation-duration: 0.01ms !important;
       animation-iteration-count: 1 !important;
       transition-duration: 0.01ms !important;
       scroll-behavior: auto !important;
     }
   }
   ```

5. Create `src/lib/cn.ts`:
   ```typescript
   import { clsx, type ClassValue } from "clsx";
   import { twMerge } from "tailwind-merge";
   export function cn(...inputs: ClassValue[]) {
     return twMerge(clsx(inputs));
   }
   ```

6. Create `src/lib/constants.ts`:
   ```typescript
   export const SECTION_IDS = [
     "hero", "about", "skills", "experience",
     "projects", "achievements", "contact"
   ] as const;
   export type SectionId = (typeof SECTION_IDS)[number];

   export const SECTION_KANJI: Record<SectionId, string> = {
     hero: "門", about: "道", skills: "技", experience: "戦",
     projects: "作", achievements: "誉", contact: "結"
   };

   export const NAV_LINKS: { id: SectionId; label: string }[] = [
     { id: "about", label: "About" },
     { id: "skills", label: "Skills" },
     { id: "experience", label: "Experience" },
     { id: "projects", label: "Projects" },
     { id: "achievements", label: "Achievements" },
     { id: "contact", label: "Contact" },
   ];
   ```

7. Create `vitest.config.ts`:
   ```typescript
   import { defineConfig } from "vitest/config";
   import path from "path";
   export default defineConfig({
     test: {
       environment: "jsdom",
       globals: true,
       setupFiles: [],
     },
     resolve: {
       alias: { "@": path.resolve(__dirname, "./src") },
     },
   });
   ```

8. Create `tests/theme/design-tokens.test.ts` that verifies globals.css contains all required tokens:
   - Test: file contains `--color-bg: #0a0a0a`
   - Test: file contains `--color-accent-red: #c0392b`
   - Test: file contains `--color-accent-gold: #d4a574`
   - Test: file contains `--color-paper: #f5f0e8`
   - Test: file contains `--font-heading`
   - Test: file contains `--font-body`
   - Test: file contains `prefers-reduced-motion: reduce`
   (Read the CSS file as text and assert string presence -- simple and reliable)
  </action>
  <verify>
    <automated>npx vitest run tests/theme/design-tokens.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next" dependency
    - package.json contains "gsap", "motion", "lenis", "next-intl", "clsx", "tailwind-merge"
    - package.json contains "vitest" in devDependencies
    - src/app/globals.css contains "--color-bg: #0a0a0a"
    - src/app/globals.css contains "--color-accent-red: #c0392b"
    - src/app/globals.css contains "--color-accent-gold: #d4a574"
    - src/app/globals.css contains "--color-paper: #f5f0e8"
    - src/app/globals.css contains "--font-heading: var(--font-playfair)"
    - src/app/globals.css contains "--font-kanji: var(--font-noto-serif-jp)"
    - src/app/globals.css contains "--font-body: var(--font-inter)"
    - src/app/globals.css contains "prefers-reduced-motion: reduce"
    - src/app/fonts.ts contains "Playfair_Display"
    - src/app/fonts.ts contains "Noto_Serif_JP"
    - src/app/fonts.ts contains "preload: false"
    - src/lib/cn.ts contains "export function cn"
    - src/lib/constants.ts contains "SECTION_IDS"
    - vitest.config.ts contains "jsdom"
    - tests/theme/design-tokens.test.ts exits 0
  </acceptance_criteria>
  <done>
    Next.js 15 project scaffolded with all dependencies installed. Three fonts configured with correct CSS variables. Full dark theme design tokens in globals.css @theme block. cn() utility and constants created. Vitest configured and design token tests passing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Set up next-intl i18n routing with TR/EN locales, message files, and layout integration</name>
  <files>
    src/i18n/routing.ts, src/i18n/request.ts, src/i18n/navigation.ts,
    src/middleware.ts, next.config.ts,
    src/messages/en.json, src/messages/tr.json,
    src/app/[locale]/layout.tsx, src/app/[locale]/page.tsx,
    tests/i18n/locale-switch.test.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (next-intl pattern, middleware config, layout code example, pitfall 5: setRequestLocale)
    src/app/fonts.ts (font variables to apply in layout)
    src/app/globals.css (design tokens to verify Tailwind utilities work)
    src/lib/constants.ts (SECTION_IDS for page structure)
  </read_first>
  <action>
1. Create `src/i18n/routing.ts`:
   ```typescript
   import { defineRouting } from "next-intl/routing";
   export const routing = defineRouting({
     locales: ["en", "tr"],
     defaultLocale: "en",
   });
   ```

2. Create `src/i18n/navigation.ts`:
   ```typescript
   import { createNavigation } from "next-intl/navigation";
   import { routing } from "./routing";
   export const { Link, redirect, usePathname, useRouter, getPathname } =
     createNavigation(routing);
   ```

3. Create `src/i18n/request.ts`:
   ```typescript
   import { getRequestConfig } from "next-intl/server";
   import { hasLocale } from "next-intl";
   import { routing } from "./routing";

   export default getRequestConfig(async ({ requestLocale }) => {
     const requested = await requestLocale;
     const locale = hasLocale(routing.locales, requested)
       ? requested
       : routing.defaultLocale;
     return {
       locale,
       messages: (await import(`../../messages/${locale}.json`)).default,
     };
   });
   ```

4. Create `src/middleware.ts`:
   ```typescript
   import createMiddleware from "next-intl/middleware";
   import { routing } from "./i18n/routing";

   export default createMiddleware(routing);

   export const config = {
     matcher: "/((?!api|trpc|_next|_vercel|.*\\..*).*)",
   };
   ```

5. Update `next.config.ts` to wrap with next-intl:
   ```typescript
   import { NextConfig } from "next";
   import createNextIntlPlugin from "next-intl/plugin";

   const nextConfig: NextConfig = {};
   const withNextIntl = createNextIntlPlugin();

   export default withNextIntl(nextConfig);
   ```

6. Create `src/messages/en.json` with initial keys:
   ```json
   {
     "nav": {
       "about": "About",
       "skills": "Skills",
       "experience": "Experience",
       "projects": "Projects",
       "achievements": "Achievements",
       "contact": "Contact",
       "downloadCV": "Download CV"
     },
     "hero": {
       "greeting": "Hello, I'm",
       "name": "Emirhan Kaya",
       "role": "Mid-Level Backend Developer",
       "tagline": "Crafting robust systems with precision and purpose"
     },
     "meta": {
       "title": "Emirhan Kaya - Portfolio",
       "description": "Mid-Level Backend Developer specializing in Java/Spring Boot and .NET"
     }
   }
   ```

7. Create `src/messages/tr.json` with matching keys:
   ```json
   {
     "nav": {
       "about": "Hakkimda",
       "skills": "Yetenekler",
       "experience": "Deneyim",
       "projects": "Projeler",
       "achievements": "Basarilar",
       "contact": "Iletisim",
       "downloadCV": "CV Indir"
     },
     "hero": {
       "greeting": "Merhaba, ben",
       "name": "Emirhan Kaya",
       "role": "Mid-Level Backend Developer",
       "tagline": "Saglam sistemleri hassasiyet ve amacla olusturuyorum"
     },
     "meta": {
       "title": "Emirhan Kaya - Portfolyo",
       "description": "Java/Spring Boot ve .NET uzmani Mid-Level Backend Developer"
     }
   }
   ```

8. Create `src/app/[locale]/layout.tsx` -- the root locale layout:
   ```typescript
   import type { Metadata } from "next";
   import { inter, playfair, notoSerifJP } from "../fonts";
   import { NextIntlClientProvider } from "next-intl";
   import { getMessages, getTranslations, setRequestLocale } from "next-intl/server";
   import { routing } from "@/i18n/routing";
   import { notFound } from "next/navigation";
   import { hasLocale } from "next-intl";
   import "../globals.css";

   export function generateStaticParams() {
     return routing.locales.map((locale) => ({ locale }));
   }

   export async function generateMetadata({
     params,
   }: {
     params: Promise<{ locale: string }>;
   }): Promise<Metadata> {
     const { locale } = await params;
     const t = await getTranslations({ locale, namespace: "meta" });
     return {
       title: t("title"),
       description: t("description"),
     };
   }

   export default async function LocaleLayout({
     children,
     params,
   }: {
     children: React.ReactNode;
     params: Promise<{ locale: string }>;
   }) {
     const { locale } = await params;
     if (!hasLocale(routing.locales, locale)) notFound();

     setRequestLocale(locale);
     const messages = await getMessages();

     return (
       <html
         lang={locale}
         className={`${inter.variable} ${playfair.variable} ${notoSerifJP.variable}`}
       >
         <body className="bg-bg text-text-primary font-body antialiased">
           <NextIntlClientProvider messages={messages}>
             {children}
           </NextIntlClientProvider>
         </body>
       </html>
     );
   }
   ```
   CRITICAL: Do NOT add `"use client"` to layout.tsx -- it must remain a Server Component.
   CRITICAL: `params` is a Promise in Next.js 15 -- must await it.
   CRITICAL: Call `setRequestLocale(locale)` before any getTranslations/getMessages.

9. Create `src/app/[locale]/page.tsx` -- placeholder page with 7 section shells:
   ```typescript
   import { setRequestLocale } from "next-intl/server";
   import { useTranslations } from "next-intl";
   import { SECTION_IDS, SECTION_KANJI } from "@/lib/constants";

   export default async function HomePage({
     params,
   }: {
     params: Promise<{ locale: string }>;
   }) {
     const { locale } = await params;
     setRequestLocale(locale);

     return (
       <main>
         {SECTION_IDS.map((id) => (
           <section
             key={id}
             id={id}
             data-section
             className="min-h-screen flex items-center justify-center"
           >
             <div className="text-center">
               <span className="font-kanji text-display text-accent-gold opacity-20">
                 {SECTION_KANJI[id]}
               </span>
               <h2 className="font-heading text-h2 text-text-primary mt-4 capitalize">
                 {id}
               </h2>
             </div>
           </section>
         ))}
       </main>
     );
   }
   ```
   NOTE: page.tsx is a Server Component. The `useTranslations` import can be used in child client components later. For now, sections just show kanji + section name as placeholders.

10. Delete default `src/app/layout.tsx` and `src/app/page.tsx` if they exist (create-next-app generates them, but we use [locale] versions instead).

11. Create `tests/i18n/locale-switch.test.ts`:
    - Test: en.json contains all required nav keys ("about", "skills", "experience", "projects", "achievements", "contact", "downloadCV")
    - Test: tr.json contains all the same keys as en.json (structural parity)
    - Test: routing.ts exports locales array containing "en" and "tr"
    - Test: middleware.ts imports from routing
    (Read files as text, parse JSON, verify structure -- no need to render React)
  </action>
  <verify>
    <automated>npx vitest run tests/i18n/locale-switch.test.ts --reporter=verbose && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - src/i18n/routing.ts contains "defineRouting"
    - src/i18n/routing.ts contains "'en', 'tr'" or '"en", "tr"'
    - src/i18n/request.ts contains "getRequestConfig"
    - src/i18n/navigation.ts contains "createNavigation"
    - src/middleware.ts contains "createMiddleware"
    - src/middleware.ts contains "matcher"
    - next.config.ts contains "createNextIntlPlugin"
    - src/messages/en.json contains "downloadCV"
    - src/messages/tr.json contains "downloadCV"
    - src/messages/tr.json contains "Hakkimda"
    - src/app/[locale]/layout.tsx contains "NextIntlClientProvider"
    - src/app/[locale]/layout.tsx contains "setRequestLocale"
    - src/app/[locale]/layout.tsx contains "generateStaticParams"
    - src/app/[locale]/layout.tsx does NOT contain "use client"
    - src/app/[locale]/page.tsx contains "data-section"
    - src/app/[locale]/page.tsx contains "SECTION_IDS"
    - npx next build exits 0
    - tests/i18n/locale-switch.test.ts exits 0
  </acceptance_criteria>
  <done>
    Complete next-intl i18n routing working with TR/EN. Middleware routes requests to [locale]. Layout wraps children with NextIntlClientProvider. Both message files have all nav keys. Page renders 7 section placeholders with kanji and section names. App builds without errors.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass
- `npx next build` -- builds with zero errors
- Visit http://localhost:3000 -- redirects to /en with 7 section placeholders
- Visit http://localhost:3000/tr -- shows Turkish locale
- Design tokens visible in browser DevTools (CSS custom properties on :root)
</verification>

<success_criteria>
1. Next.js 15 app builds and runs with zero TypeScript errors
2. /en and /tr routes both render 7 section placeholder shells
3. All design token CSS custom properties defined in globals.css @theme block
4. Three fonts (Inter, Playfair Display, Noto Serif JP) loaded via next/font/google
5. Vitest configured and all tests passing
6. en.json and tr.json have structural parity for all nav keys
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
