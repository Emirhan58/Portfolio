import { describe, it, expect } from "vitest";

describe("SakuraFall (refined)", () => {
  it.todo("generates 20-25 petals (not 50)");
  it.todo("petals have opacity between 0.15 and 0.25");
  it.todo("does not render when shouldParticle is false");
  it.todo("renders with aria-hidden=true");
});
