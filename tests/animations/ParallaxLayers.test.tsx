import { describe, it, expect } from "vitest";

describe("ParallaxLayers", () => {
  it.todo("renders mountain, cloud, and mist layers");
  it.todo("does not render when shouldParallax is false");
  it.todo("renders with aria-hidden=true");
});
