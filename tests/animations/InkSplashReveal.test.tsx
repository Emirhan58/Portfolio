import { describe, it, expect } from "vitest";

describe("InkSplashReveal", () => {
  it.todo("renders children content");
  it.todo("wraps children in a container div with ref");
  it.todo("does not apply animation when shouldAnimate is false");
});
