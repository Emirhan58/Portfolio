import { describe, it, expect } from "vitest";

describe("StatCounter", () => {
  it.todo("renders initial value 0 when shouldAnimate is true");
  it.todo("renders final value directly when shouldAnimate is false");
  it.todo("accepts value and suffix props");
});
