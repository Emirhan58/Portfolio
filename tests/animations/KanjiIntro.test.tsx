import { describe, it, expect } from "vitest";

describe("KanjiIntro", () => {
  it.todo("renders overlay when sessionStorage has no kanji-intro-seen key");
  it.todo("does not render overlay when sessionStorage has kanji-intro-seen key");
  it.todo("renders skip button with text 'Skip'");
  it.todo("sets sessionStorage kanji-intro-seen on skip click");
  it.todo("renders SVG with kanji-stroke class paths");
  it.todo("does not render when shouldAnimate is false (none tier)");
});
